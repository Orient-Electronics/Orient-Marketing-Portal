<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Orient" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= yield :head %>
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true">
    </script>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.7/jquery.min.js">
    </script>
  </head>
  <body>
    <% if user_signed_in? %>
      <div id="page-container" class="sidebar-partial sidebar-visible-lg sidebar-no-animations">
        <%#= render :partial => "layouts/alternet_slider"%>
        <% if user_signed_in? %> 
          <%= render :partial => "/layouts/header" %>
        <% end -%>
        <!-- Main Container -->
        <div id="main-container">
          <!-- Header -->
          <% if user_signed_in? %> 
            <%= render :partial => "/layouts/main_header" %> 
          <% end -%>  
          <!-- END Header -->
          <div id="page-content" >
            
            
            <%= bootstrap_flash %>
            <%= yield %>
          </div><!--/row-->
          <!-- Footer -->
            <footer class="clearfix">
              <div class="pull-right">
                  by <a href="http://www.phaedrasolutions.com" target="_blank">phaedra solutions</a>
              </div>
              <div class="pull-left">
                  <span id="year-copy"></span> &copy; <a href="http://orient.com.pk/" target="_blank">Orient</a>
              </div>
            </footer>
          <!-- END Footer -->

        </div> <!-- /container -->

        <!-- Javascripts
        ================================================== -->
        <!-- Placed at the end of the document so the pages load faster -->
        <%= javascript_include_tag "application" %>
        <%= yield :footer_js %>
      </div>
   <% else %>
    <!-- Login Background -->
    <div id="login-background">
    <!-- For best results use an image with a resolution of 2560x400 pixels (prefer a blurred image for smaller file size) -->
      <img src="/assets/placeholders/headers/login_header.jpg" alt="Login Background" class="animation-pulseSlow">
    </div>
    <%= yield %>
   <% end %> 
    
    <!-- Scroll to top link, initialized in js/app.js - scrollToTop() -->
      <a href="#" id="to-top"><i class="fa fa-angle-double-up"></i></a>
    <!-- User Settings, modal which opens from Settings link (found in top right user menu)    -->
  </body>
</html>
