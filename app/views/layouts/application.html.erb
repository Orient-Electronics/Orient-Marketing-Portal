<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Orient" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= yield :head %>
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=true"></script>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.7/jquery.min.js"></script>
  </head>
  <body class="<%= 'login' unless user_signed_in? %>">
    <div id="page-container" class="sidebar-partial sidebar-visible-lg sidebar-no-animations">
      <%#= render :partial => "layouts/alternet_slider"%>
      <% if user_signed_in? %> 
        <%= render :partial => "/layouts/header" %>
        <% unless Announcement.all.blank? %>
          <% if current_user.can_view_announcement? %>
            <div class="announcement-parent"> 
              <button class="announcement-dismiss close">×</button>
              <div class="announcement text-center alert-error"> <marquee behavior="scroll" direction="left" onmouseover="this.stop();" onmouseout="this.start();"> <%= Announcement.last.try(:created_at).strftime("%d %B, %Y %H:%M %p") %> <%= Announcement.last.try(:announcement) %> </marquee></div>
            </div>
          <% end %>  
        <% end -%>  
      <% end -%>
      <!-- Main Container -->
      <div id="main-container">
        <!-- Header -->
          <%#= render :partial => "/layouts/main_header" %>
        <!-- END Header -->
        <!-- <div class="row-fluid">
          <div class="span12">
            <%#= bootstrap_flash %>
            <%#= yield %>
          </div> -->
        </div><!--/row-->
      </div> <!-- /container -->

      <!-- Javascripts
      ================================================== -->
      <!-- Placed at the end of the document so the pages load faster -->
      <%= javascript_include_tag "application" %>
      <%= yield :footer_js %>
    </div>
    <!-- Scroll to top link, initialized in js/app.js - scrollToTop() -->
      <a href="#" id="to-top"><i class="fa fa-angle-double-up"></i></a>
    <!-- User Settings, modal which opens from Settings link (found in top right user menu)    -->
  </body>
</html>
