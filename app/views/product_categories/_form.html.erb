<%= form_for @product_category, :html => { :class => 'form-horizontal' } do |f| %>
  <div class= "error-box">
    <p><%= errors_for(@product_category, "unable to add new product category").html_safe %></p>
  </div>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>

  <div class="control-group">
    <%= hidden_field_tag "product_category[brand_ids][]", nil %>
    <div class="controls">
      <% Brand.all.each do |brand| %>
        <%= check_box_tag "product_category[brand_ids][]", brand.id, @product_category.brand_ids.include?(brand.id), id: dom_id(brand), :class => 'pull-left' %>
        <%= label_tag dom_id(brand), brand.name %>
      <% end %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                product_categories_path, :class => 'btn' %>
  </div>
<% end %>
